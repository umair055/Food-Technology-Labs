import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#05C26A",
    },
    success: { main: "#05C26A", light: "#05C26A1A" },
  },
});

export default theme;
